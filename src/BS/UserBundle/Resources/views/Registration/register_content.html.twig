{% trans_default_domain 'FOSUserBundle' %}

{% form_theme form ":FormTemplate:fosuserbundle_perso.html.twig" %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="form-button" value="{{ 'registration.submit'|trans }}" />
    </div>
{{ form_end(form) }}
