{% extends "BSTchatBundle::layout.html.twig" %}

{% block title %} {{ parent() }} {% endblock %}

{% block stylesheets %} {{ parent() }} {% endblock %}

{% block body %}
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
    <div class="container-fluid">
        <div class="row">
            <nav class="col-md-12" id="main-nav">
                <div id="nav-items" class="text-right">
                    <span id="counter">5</span><a href="{{ path('fos_user_security_logout') }}" class="nav-button text-center">Déconnexion</a>
                </div>
            </nav>
        </div>
        <div class="row">
            <div class="col-sm-3" id="login-area">

            </div>
            <div id="tchat-area" class="col-sm-offset-3 col-sm-9">
                <form class="col-sm-12" id="tchat-form">
                    <p>
                        <input type="text" name="message" class="col-sm-10" id="message" />
                        <input type="submit" value="Send" class="col-sm-2" id="submit" />
                    </p>
                </form>
            </div>
        </div>
    </div>
    {% else %}
        <div class="container-fluid">
            <div id="home-register">
                <div id="login-or-register-area" class="col-sm-offset-4 col-sm-4">
                    <div class="row" id="registration-button">
                        <a href="{{ path('fos_user_registration_register') }}" class="login-button col-sm-offset-3 col-sm-6 text-center">Inscription</a>
                    </div>
                    <div class="row" id="connexion-button">
                        <a href="{{ path('fos_user_security_login') }}" class="login-button col-sm-offset-3 col-sm-6 text-center">Connexion</a>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}

{% block javascripts %} {{ parent() }} {% endblock %}